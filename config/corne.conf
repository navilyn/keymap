CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5  
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

CONFIG_ZMK_BATTERY_REPORT_INTERVAL=60000  // Reports battery % every 60s (default: 30s)  
CONFIG_ZMK_BATTERY_REPORTING=y  

CONFIG_ZMK_SLEEP=y  
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000  // 15 min inactivity → sleep  
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y  // Max power (some boards only support +4)

CONFIG_BT_PERIPHERAL_PREF_MAX_INT=6  // Lower = more responsive but higher power  
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6

CONFIG_BT_MAX_CONN=1  // Only 1 connection (if not pairing to multiple devices)  
CONFIG_BT_MAX_PAIRED=5
